# Base requirements needed to run this application
--allow-all-external
Django>=1.7                             # The web application framework used for this project
MySQL-python>=1.2.5                     # Database library used to communicate with MySQL
Pillow>=2.6.0                           # Python Imaging Library - a fork that has built-in image support is maintained more often
django-allauth>=0.18.0                  # Django auth module for local and social account registration
django-pipeline>=1.3.25                 # Asset (Javascript and CSS) compilation, minification and bundling
djangorestframework>=2.4.3              # API rest framework that coordinates with Django's subsystems
oauthlib>=0.6.3                         # requests-oauthlib dependency (and therefore also a django-allauth dependency)
python-openid>=2.2.5                    # Python OpenID library (django-allauth dependency)
pytz>=2014.7                            # Python timezone library (Django recommended)
requests-oauthlib>=0.4.1                # django-allauth dependency
requests>=2.4.1                         # HTTP library for Python (allauth dependency)
markdown>=2.5.1                          # Used for API documentation generation

# Other goodies
# django-filter>=0.7                      # Used in conjunction with Django Rest Framework for providing more restrictive API filtering
# supervisor>=3.1.2                       # A process control system
# raven>=5.0.0                            # Client for sentry logging
# boto>=2.32.1                            # Python interface for Amazon Web Services
# gunicorn>=19.1.1                        # WSGI server (this is typically used in a reverse-proxy scenario)
# django-storages>=1.1.8                  # A library that provides storage scaffolding for other than the local filesystem
# django-redis>=3.7.1                     # Django redis caching
# redis>=2.10.3                           # Python extensions for Redis server communication
# django-autoslug>=1.7.2                  # Provides an auto-slug model fields for Django models
# django-grappelli>=2.6.1                 # Admin site interface extensions
# django-guardian>=1.2.4                  # Object level permissions for Django
# django-jsonfield>=0.9.13                # A JSON-based model field for use with Django models (django-activity-stream)
# django-localflavor>=1.0                 # Country-specific Django helpers, formerly of contrib fame
